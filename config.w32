// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("concurrency", "for concurrency support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("concurrency", "enable concurrency support", "no");

if (PHP_CONCURRENCY != "no") {
	EXTENSION("concurrency", "concurrency.c");
}

